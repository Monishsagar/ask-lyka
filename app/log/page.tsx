'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
export default function LogPage(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/log').then(r=>r.json()).then(setRows)},[]);return <main className="shell"><header><Link href="/" className="brand">LYKA <span>REALTY</span></Link><nav><Link href="/">Ask Lyka</Link></nav></header><section className="log-page"><div className="eyebrow">Audit trail</div><h1>Question log</h1><p className="lede">Every response, including declines, is recorded with its verification work.</p><div className="table-wrap"><table><thead><tr><th>Question</th><th>Outcome</th><th>Citation</th><th>Reason</th></tr></thead><tbody>{rows.map((r,i)=><tr key={i}><td>{r.question}</td><td><span className="pill">{r.outcome}</span></td><td>{r.citations?.map((c:any)=>`${c.id}.${c.field}`).join(', ')||'—'}</td><td>{r.reason}</td></tr>)}</tbody></table>{!rows.length&&<p className="empty">No questions logged in this server session.</p>}</div></section></main>}
