# Disable pnpm's minimumReleaseAge supply-chain check for this project.
# The newly-published @supabase/* and vitest packages fail this check
# because they were published within the last 24 hours — disabling it here
# lets the build run without requiring a global pnpm config change.
minimum-release-age=0
